# CLAUDE.md — epe-web

Frontend de la app (React + Vite + TypeScript).

Toda instrucción, convención y decisión de arquitectura (actual y futura) para este proyecto vive en `AGENTS.md`, en la raíz de este mismo proyecto — no en este archivo.

Cualquier actualización futura de indicaciones para Claude (o cualquier otro agente) debe escribirse en `AGENTS.md`, no acá. Este archivo es solo un puntero.
